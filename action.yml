name: "Setup awesome-ci tooling"
description: "Setup awesome-ci as a CI tool in the latest stable version"
branding:
  color: blue
  icon: anchor
inputs:
  version:
    required: false
    description: "The version to use (default: 0.13.1)"
    default: "0.13.1"
  arch:
    required: false
    description: "The softwart architecture (default: amd64)"
    default: "amd64"
runs:
  using: 'node16'
  main: 'dist/index.js'
